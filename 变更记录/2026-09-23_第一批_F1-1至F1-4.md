# 变更记录：v0.4.4 第一批（阻断性）F1-1 ~ F1-4

- **日期**：2026-09-23
- **批次**：第一批（阻断性），对应 `docs/ncm2bili_技术文档.md` v0.4.4 计划表 F1-1 ~ F1-4
- **目标**：代码追平文档 v0.4.4 计划版
- **测试基线**：183 项 → 193 项（全绿）
- **约束**：全程 respx mock，无真实网络请求；技术文档为唯一语义出处

---

## 一、变更文件列表

| # | 文件 | 任务 | 变更要点 |
|---|------|------|----------|
| 1 | `matcher.py` | F1-1, F1-2 | `_search_cached` 入口下沉 sleep（`interval_ms + uniform(jitter)`，覆盖降级链每轮与缓存命中）；`_results_owned` → `_filter_owned` 逐条过滤候选；缓存读/写同规则，过滤后为空作废重取 |
| 2 | `main.py` | F1-1, F1-4 | worker 内 sleep 删除（已下沉，避免双倍间隔）；`_report_command` 捕获 token 并传入 `write_review_html` |
| 3 | `fav.py` | F1-3 | `fav_songs` 重写：按 `song_key` 排序查 DB 建 ordinal 映射，第 i 首进夹 `⌊pos/per_folder_limit⌋`（封顶末夹，禁止轮转）；`ensure_folders` 数量按 `total_matched` |
| 4 | `review_server.py` | F1-4 | `secrets.token_hex(16)` 一次性 token 注入 handler；`do_POST` 校验 Origin 同源（`http://127.0.0.1:{port}`）、X-Token 匹配、`song_key ≤ 300` 字符；返回 `(server, port, token)` |
| 5 | `report.py` | F1-4 | 按钮改 `data-song-key` 属性（`html.escape(quote=True)`）；JS 改事件委托 `addEventListener` + `btn.dataset.songKey`；fetch 带 `X-Token` 头；`write_review_html` 加 `token` 参数 |
| 6 | `tests/test_matcher.py` | F1-1, F1-2 | `_fast_cfg()` 归零限速；新增 3 测试；2 断言对齐新 spec（`res == []`、`降级链全部未命中`） |
| 7 | `tests/test_main.py` | F1-1 | `_make_components` 归零搜索限速 |
| 8 | `tests/test_fav.py` | F1-3 | 新增 2 测试（按序连续分段、resume 稳定映射） |
| 9 | `tests/test_review_server.py` | F1-1, F1-4 | `_make_config` 归零限速；`_post_json` 加 token/origin 参数；新增 5 安全测试；HTML 断言改 `data-song-key` / `onclick=` 不存在 |

另含 `docs/ncm2bili_技术文档.md`（v0.4.4 计划表修订，随本 PR 一并入库）。

## 二、测试运行结果

```
193 passed in 113.69s
```

- 基线 183 → 193（+10 新测试，零失败）
- 测试命令：`.venv\Scripts\python.exe -m pytest -o addopts="" -q --tb=short`

新增测试明细：

| 任务 | 测试 |
|------|------|
| F1-1 | `test_search_sleep_formula_covers_cache_hit_and_miss` |
| F1-2 | `test_filter_owned_keeps_only_matching_candidates`、`test_filter_owned_cache_hit_returns_only_owned` |
| F1-3 | `test_fav_songs_sequential_segmentation_no_roundrobin`、`test_fav_songs_resume_uses_total_matched_for_folders` |
| F1-4 | `test_server_rejects_missing_token` / `wrong_token` / `wrong_origin` / `missing_origin`（403）、`oversized_song_key`（400） |

## 三、与文档条款对照说明

| 任务 | 文档条款（v0.4.4 计划表 / 正文） | 落地实现 | 测试验证 |
|------|----------------------------------|----------|----------|
| **F1-1** | §7 预防层：搜索间隔补回 interval_ms，sleep 下沉到每次搜索请求（含降级链每一轮、缓存命中路径）：sleep = interval_ms + uniform(jitter) | `matcher.py` `_search_cached` 入口统一 sleep；`main.py` worker 原 sleep 删除 | monkeypatch 验证缓存命中/未命中各 sleep 一次、公式正确 |
| **F1-2** | §4.4：归属校验改逐条过滤候选（仅保留归一化标题含全部歌名 token 的候选），过滤后为空视为该关键词未命中；缓存读/写同规则 | `_filter_owned` 逐条过滤；缓存命中过滤后为空 → 作废重取；新搜索仅缓存非空 owned 结果 | 混合候选仅保留归属项；缓存命中混合同样过滤 |
| **F1-3** | §5.3：收藏夹按序连续分段（第 i 首进夹 ⌊i/900⌋+1），禁止轮转；resume 建夹数量按任务总匹配数计算，保证歌进原夹 | ordinal 映射按 `ORDER BY song_key` 稳定定序；`folder_idx = min(pos // limit, 末夹)`；建夹数 = ⌈total_matched/900⌉ | 5 首 limit=2 → 3 夹，1,2→夹1 / 3,4→夹2 / 5→夹3（非轮转）；resume 传 2 首子集仍建 3 夹且进原夹 |
| **F1-4** | §10.3：写入侧一次性 token + Origin 校验（仅 `http://127.0.0.1:<port>`）+ song_key 长度上限；前端 data-* 属性 + 事件委托，废除 onclick 拼接 | token 校验失败/Origin 非同源 → 403；song_key >300 → 400；按钮 `data-song-key` + 委托监听 + fetch 带 `X-Token` | 4 项 403 + 1 项 400 全验证；HTML 断言 `data-song-key=` 存在、`onclick=` 不存在、`addEventListener` 存在、token 注入 |

## 四、备注

- 无文档冲突项，无需差异清单决策。
- 后续批次（第二批 MATCHED 全链路等）按文档 v0.4.4 计划表继续，每批完成后在本目录生成对应变更报告。
